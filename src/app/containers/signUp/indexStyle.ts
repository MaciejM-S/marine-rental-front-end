import { theme } from "../../../theme/theme";

const indexStyle = {
  helperTextProps: {
    sx: {
      color: theme.palette.primary.main,
      fontWeight: 700,
    },
  },
  buttonStyle: { mt: 3, mb: 2, color: "white", height: "40px" },
};

export default indexStyle;
